@model List<AboutImage>
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">AboutImage </h5>
                <a class="btn btn-outline-success my-2" href="/admin/aboutimage/create">Create</a>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>

                                <th scope="col">Image</th>
                                <th scope="col">Edit</th>
                                <th scope="col">Delete</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                foreach (var aboutImage in Model)
                                {
                                    <tr>
                                        <td><img src="/assets/images/@aboutImage.FilePath" style="width:100px; height:100px;" /></td>


                                        <td> <a asp-area="Admin" asp-action="Update" asp-controller="AboutImages" asp-route-id="@aboutImage.Id">Edit</a></td>
                                        <td>
                                            <form asp-area="Admin" asp-action="Delete" asp-controller="AboutImages" asp-route-id="@aboutImage.Id" method="post">
                                                <button type="submit">Delete</button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            }


                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</div><!--End Row-->
